<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/resultImage"
        android:layout_width="180dp"
        android:layout_height="180dp"
        app:layout_constraintDimensionRatio="1:1"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:padding="8dp"
        />

    <TextView
        android:id="@+id/resultBrand"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:singleLine="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:freezesText="true"
        app:layout_constraintTop_toBottomOf="@+id/resultImage"
        app:layout_constraintLeft_toLeftOf="@+id/resultImage"
        app:layout_constraintRight_toRightOf="@+id/resultImage"
        />

    <TextView
        android:id="@+id/resultPrice"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:singleLine="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:freezesText="true"
        app:layout_constraintTop_toBottomOf="@+id/resultBrand"
        app:layout_constraintLeft_toLeftOf="@+id/resultBrand"
        app:layout_constraintRight_toRightOf="@+id/resultBrand"
        />

</androidx.constraintlayout.widget.ConstraintLayout>